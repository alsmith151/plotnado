CapCruncher fixture used by PlotNado's real matrix-track tests and examples.

- `SAMPLE-A_REP1.hdf5`: downloaded from CapCruncher's public test data and used as a reporters-store example.
- The relevant cooler group inside this HDF5 file is `/Slc25A37`, which is the viewpoint used by PlotNado's `CapcruncherTrack` tests and examples.